import 'package:flutter/material.dart';

class RoadSignScreen extends StatelessWidget {
  const RoadSignScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Road Sign Screen"));
  }
}
